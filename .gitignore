node_modules
puppeteer
dist
*.png
*.copy.*
*.working.*

# Generated declaration maps (tsc --declarationMap emits these next to the .d.ts)
*.d.ts.map

# Snapshot plugin output
.snapshots/

# Throwaway debug/repro artifacts — `pnpm git` runs `git add .`, keep them out
repro*.html
repro*-script.js
browser_ssr.txt
nodejs_ssr.txt
ssr_output.txt
dv4_console.json
.dv-session.json

# Cygwin crash dumps (git bash, dv4) -- an artifact of a tool dying, never source
*.stackdump
